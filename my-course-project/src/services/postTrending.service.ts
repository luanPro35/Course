import { CardTrending } from "@/types/trending";


export const getCourses = async (): Promise<CardTrending[]> => {
  
  
  console.warn("Trending posts feature is not available - backend endpoint missing");
  return [];

  
};
